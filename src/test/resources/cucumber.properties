cucumber.publish.quiet=true
# For Cucumber's own runner (IntelliJ: run a feature or scenario from the editor), which passes no glue.
# The package covers the acceptance steps and the browser steps in the smoke subpackage;
# the browser hooks only fire for scenarios tagged @browser.
cucumber.glue=de.sharpsharp.vendingmachine
